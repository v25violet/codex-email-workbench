# Campaign brief

campaign_id: ""

## 目标

- 目标：`""`
- 受众：`""`
- 价值主张：`""`
- 语气与语言：`""`
- CTA：`""`

## 边界

- 禁用声明或无法承诺的内容：`""`
- 可引用事实来源：`""`
- 不应写入邮件的敏感信息：`""`
- follow-up：第 2、4、7 个工作日，最多 3 次；回信、拒绝、退订、退信或暂停立即停止

## 审批

- 每行账号列：`provider` + `account_key`
- 允许发送时段与时区：`""`
- 发送节奏：`""`（必须写入预览，例如每封间隔和暂停条件）
- 模式：`draft_only` 或 `approved_batch_send`
